# Aevor Website

## Run locally

```
npm install
npm run dev
```

Then open http://localhost:5173

## Build for production

```
npm run build
npm run preview
```

The `dist/` folder contains the static site — drag it to Netlify Drop to publish.
